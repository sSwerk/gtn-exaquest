<div class="dashboardcard">
    <form method="post" action="{{action}}">
        <p>{{#str}}
            request_questions_label, block_exaquest {{/str}}
            <br>
            <a class="selectallornone">selectallornone</a>
            {{#fragenersteller}}
                <!--                TODO: Select all/none -->
                <br>
                <!--                {{firstname}} {{lastname}}{{#comma}},{{/comma}}{{^comma}}.{{/comma}}-->
                <input type="checkbox" id={{id}} name="selectedusers[{{id}}]" value={{id}}>
                <label for="selectedusers[{{id}}]">{{firstname}} {{lastname}}</label>
            {{/fragenersteller}}
        </p>
        <input type="submit" value="{{#str}} request_questions_button, block_exaquest {{/str}}"
               class="btn btn-primary">
    </form>
</div>